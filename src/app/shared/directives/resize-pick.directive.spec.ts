import { ResizePickDirective } from './resize-pick.directive';

describe('ResizePickDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizePickDirective();
    expect(directive).toBeTruthy();
  });
});
